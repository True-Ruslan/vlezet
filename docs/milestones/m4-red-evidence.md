# M4 RED evidence

The first M4 CI run is expected to fail because tests already require APIs that do not exist yet:

- project storage version 2 and calibrated reference metadata;
- project asset repository and safe replacement transaction;
- calibrated image-pixel/world geometry.

Production implementation follows only after the failing run confirms the tests are active.
