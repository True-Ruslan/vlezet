# M7.8B — Source Normalisation and Wall Topology

**Status:** AUTOMATED REMEDIATION PASS / PRODUCT OWNER RE-REVIEW REQUIRED  
**Date:** 2026-08-02  
**Branch:** `feat/m7-8b-source-normalization-wall-topology`  
**PR:** #41  
**Base:** `d6e8668c5ad0780a0a28d9c1fef6e9d37e9bbe4d`  
**Product implementation head:** `f2694519fce95ac02a3f43e916f5b6c96d133c36`

## Summary

M7.8B remains unaccepted pending repeat product-owner verification on the representative real plan.

The first implementation produced 417 local wall candidates from furniture, sanitary symbols, labels, digits and door arcs. Region-first remediation reduced the same real workflow to a reviewable local Draft, although one real wall remains missed.

A subsequent GPT-4o check was materially worse than the local result: it added long unsupported lines and unrelated topology. The AI review path has therefore been changed from free reconstruction to strict verification of existing local candidates.

The private plan and screenshots were not committed.

## Region-first local recognition

- Otsu binary inversion and bounded morphological opening produce a thick structural raster.
- A deterministic scanner extracts stable horizontal and vertical filled wall bands.
- Bands must satisfy overlap, run-length similarity, physical thickness, minimum length and aspect-ratio constraints.
- Each accepted wall region becomes exactly two boundary segments for deterministic centreline/topology construction.
- Canny/Hough runs only as a fallback when fewer than three structural regions exist.
- Local candidate explosions above 80 walls fail closed before persistence and AI review.
- Persisted overloaded Drafts are sanitised on restore.
- Local openings remain deferred until M7.8C.

## Verification-only AI review

The previous provider prompt sent the image and only the number of local candidates. GPT-4o therefore re-recognised the plan instead of checking the local Draft.

The updated path now:

- sends every local wall ID and exact coordinates;
- forbids new wall IDs and geometry;
- requires returned walls to preserve ID and coordinates;
- allows AI only to confirm/reject candidates and provide confidence/score;
- requires `openings: []` in M7.8B;
- rejects unknown cloud IDs before reconciliation;
- rejects known IDs with substituted geometry;
- rejects unsupported full-frame lines and candidate explosions;
- never appends unmatched cloud walls/openings;
- never moves local wall endpoints;
- preserves local geometry as the only recognition authority.

Consequently, AI can no longer hide a missed local wall by inventing topology. Missing-wall improvement remains a local CV/topology task.

## Regression authority

The corpus contains nine fixtures. `clutter-symbol-regression` is repository-owned synthetic data containing large digits, furniture, sanitary symbols, door arcs, labels and hatching. It contains no user raster, dimensions or identifiers.

## Metrics

| Metric | Previous post-failure Source | Region-first Core | Region-first Source |
| --- | ---: | ---: | ---: |
| Wall geometry F1 | `0.492537` | `0.855615` | `0.837989` |
| Wall topology F1 | `0.462687` | `0.834225` | `0.837989` |
| Incorrect high-confidence rate | `0` | `0` | `0` |
| Unknown-host openings | `0` | `0` | `0` |
| Stale decisions | `0` | `0` | `0` |

Dense anonymised source:

```text
geometry/topology F1: 0.880000 / 0.880000
TP / FP / FN:         11 / 2 / 1
```

Clutter-symbol source:

```text
selected mode:        regions
structural regions:   19
wall candidates:      12
geometry/topology F1: 1.000000 / 1.000000
TP / FP / FN:         12 / 0 / 0
overload:              false
```

## Engine and baseline

- recognition engine version: `5`;
- Worker, controller and benchmark consume one public engine-version source;
- nine-fixture baseline migration is explicit and reviewed;
- baseline auto-update remains forbidden.

## Exact product-head verification

```text
head:                    f2694519fce95ac02a3f43e916f5b6c96d133c36
Standard CI:             30758342299 / #2774 — PASS
Recognition Benchmark:  30758342317 / #135 — PASS
M7 Browser Audit:        30758342345 / #588 — PASS
benchmark artifact:      8836651302
benchmark digest:        sha256:24fae84aa1e3233aa87efdc1555643f0e749d0e8f0cc8eb68b2f3244af43bf5e
```

The exact product head passed the complete unit suite, provider prompt contracts, sanitizer/reconciliation tests, Core baseline comparison, nine Chromium/OpenCV fixtures and overlays, Worker equality, TypeScript, ESLint, production build, Chromium regression and WebKit smoke. `sha256sum -c SHA256SUMS` passes for every benchmark evidence file.

## Remaining limitations

- aggregate Source wall-topology F1 remains below the final `0.90` target;
- the representative real plan still misses one wall locally;
- perspective-photo recognition remains unresolved at `0/0`;
- doors/windows, room faces, OCR labels and areas are later M7.8 slices;
- AI is deliberately unable to create missing geometry in M7.8B;
- automated evidence does not override the product-owner gate.

## Re-review requirement

The same real plan must confirm that local recognition remains reviewable and that GPT-4o no longer changes the wall count, coordinates or topology, adds no long unsupported lines, and adds no openings. Only literal acceptance may make PR #41 Ready and permit merge.
